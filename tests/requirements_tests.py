import pytest


def test_pyfa_fitting_block_read():
    """
    Tests that it can read a fitting block
    :return:
    """
    assert False


def test_track_fit_quota():
    """
    Test if the quota file keeps track of all lines there
    CSV with ;
    """
    assert False


def test_count_number_of_ship_fit_availability():
    """
    count how many ships have the fit's name in a hangar (or text copied/pasted from a hangar) and compare against
    the target number
    """
    assert False


def test_count_difference_fits_to_assemble_versus_fits_already_existing():
    """
    count how many ships have the fit's name in a hangar (or text copied/pasted from a hangar) and compare against
    the target number 
    """
    assert False


def test_return_number_of_fits():
    """
    - return how many of each fit to assemble
        - How many r-click multifits from target number
    """
    assert False


def test_get_global_fit_multiplier():
    """
    Gets the global fit multiplier
    :return:
    """
    assert False


def test_get_global_material_multiplier():
    """
    Tests the getting of the global material multiplier
    :return:
    """
    assert False


def test_test_global_fit_multiplier_for_fits():
    """
    Test the global fit multiplier works for all fits
    :return:
    """
    assert False


def test_get_global_material_multiplier():
    """
    Tests that the global material multiplier works for fits then converted materials * multiplier
    :return:
    """
    assert False


def test_account_for_station():
    """
    Check should account for individual station set ups
    :return:
    """
    assert False
